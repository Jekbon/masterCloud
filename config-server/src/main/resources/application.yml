spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jekbon/springcloudConfig.git
          search-paths:
server:
  port: 8888
